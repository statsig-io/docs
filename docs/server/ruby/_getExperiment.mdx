```ruby
# Values via getLayer

layer = Statsig.get_layer(user, "user_promo_experiments")
title = layer.get("title", "Welcome to Statsig!")
discount = layer.get("discount", 0.1)

# or, via getExperiment

title_exp = Statsig.get_experiment("new_user_promo_title")
price_exp = Statsig.get_experiment("new_user_promo_price")

title = title_exp.getString("title", "Welcome to Statsig!")
discount = price_exp.getDouble("discount", 0.1)

...

price = msrp * (1 - discount)


```
