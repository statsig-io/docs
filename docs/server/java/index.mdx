import Template from '../_server-sdk-template.mdx'

import Install from './_install.mdx'
import Initialize from './_initialize.mdx'
import CheckGate from './_checkGate.mdx'
import GetConfig from './_getConfig.mdx'
import GetExperiment from './_getExperiment.mdx'
import LogEvent from './_logEvent.mdx'
import Options from './_options.mdx'
import FAQs from './_faqs.mdx'
import Shutdown from './_shutdown.mdx'

:::info Server vs. Client
These docs are for using our Java/Kotlin SDK in a multi-user, server side context. For client side and single-user contexts, check out one of the other client SDKs.
:::

This SDK is written in Kotlin, but exposes methods and overrides to Java based applications.

<Template
  sdkType="Java/Kotlin"
  github="https://github.com/statsig-io/java-server-sdk"
  install={<Install />}
  initialize={<Initialize />}
  checkGate={<CheckGate />}
  getConfig={<GetConfig />}
  getExperiment={<GetExperiment />}
  logEvent={<LogEvent />}
  options={<Options />}
  faqs={<FAQs />}
  shutdown={<Shutdown />}
/>
