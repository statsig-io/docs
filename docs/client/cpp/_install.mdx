import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

To include the Statsig C++ SDK in your project. You can download it from GitHub or use CMake's FetchContent.

Then in your CMakeLists.txt file, do the following:

<Tabs
  groupId="cpp-install-source"
  defaultValue="github"
  values={[
    {label: 'Downloaded From GitHub', value: 'github'},
    {label: 'FetchContent', value: 'fetch-content'},
  ]}>

  <TabItem value="github">

```cpp
add_subdirectory(path/to/downloaded/statsig_sdk)
target_link_libraries(${PROJECT_NAME} StatsigClientSDK)
```

  </TabItem>
  <TabItem value="fetch-content">

```cpp
include(FetchContent)

FetchContent_Declare(statsig
  GIT_REPOSITORY    https://github.com/statsig-io/cpp-client-sdk.git
  GIT_TAG           main
)
FetchContent_MakeAvailable(statsig)

target_link_libraries(${PROJECT_NAME} StatsigClientSDK)
```

  </TabItem>
</Tabs>
