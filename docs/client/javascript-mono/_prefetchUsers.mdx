import GitHubEmbed from "@site/src/components/GitHubEmbed";


### Prefetching Users

In some usecases, it can be useful to prefetch values for a given user, such that switching 
to that user later can be done synchronously.

To do this, you would call `prefetchData` and then later `updateUserSync`.

<GitHubEmbed url="https://raw.githubusercontent.com/statsig-io/js-client-monorepo/main/samples/react/src/samples/precomputed-client/sample-precomp-prefetch.tsx" />
