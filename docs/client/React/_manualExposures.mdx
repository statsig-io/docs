import CodeBlock from "@theme/CodeBlock";

export const Snippets = {
  // Gates
  gateSnippet: (
    <CodeBlock language="swift">{`const { value } = useGateWithExposureLoggingDisabled("a_gate_name");`}</CodeBlock>
  ),
  gateExposureSnippet: (
    <CodeBlock language="swift">{`Statsig.manuallyLogGateExposure("a_gate_name");`}</CodeBlock>
  ),
  // Configs
  configSnippet: (
    <CodeBlock language="swift">{`const { config } = useConfigWithExposureLoggingDisabled(
    "a_dynamic_config_name"
);`}</CodeBlock>
  ),
  configExposureSnippet: (
    <CodeBlock language="swift">{`Statsig.manuallyLogConfigExposure("a_dynamic_config_name");`}</CodeBlock>
  ),
  // Experiment
  experimentSnippet: (
    <CodeBlock language="swift">{`const { config } = useExperimentWithExposureLoggingDisabled("an_experiment_name");`}</CodeBlock>
  ),
  experimentExposureSnippet: (
    <CodeBlock language="swift">{`Statsig.manuallyLogExperimentExposure("an_experiment_name", false);`}</CodeBlock>
  ),
  // Layer
  layerSnippet: (
    <CodeBlock language="swift">{`const { layer } = useLayerWithExposureLoggingDisabled("a_layer_name");
const result = layer.getValue("a_parameter_name", "fallback");`}</CodeBlock>
  ),
  layerExposureSnippet: (
    <CodeBlock language="swift">{`Statsig.manuallyLogLayerParameterExposure("a_layer_name", "a_parameter_name");`}</CodeBlock>
  ),
};
