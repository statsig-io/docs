```lua
local config = Statsig:GetConfig("awesome_product_details")

-- The 2nd parameter is the default value to be used in case the given parameter name does not exist on
-- the Dynamic Config object. This can happen when there is a typo, or when the user is offline and the
-- value has not been cached on the client.
local itemName: string = config:get("product_name", "Awesome Product v1")
local price: number = config:get("price", 10.0)
local shouldDiscount: boolean = config:get("discount", false)
```
