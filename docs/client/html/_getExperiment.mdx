```js
const promoLayer = window.__STATSIG__.instance().getLayer("user_promo_experiments");

const title = promoLayer.get("new_user_promo_title", "");
const price = promoLayer.get("new_user_promo_price", 0);
```