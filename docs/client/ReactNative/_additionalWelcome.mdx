If you are using Expo, check out our [Expo SDK](./reactNativeExpoSDK).

This SDK (`statsig-react-native`) wraps the [statsig-react](./reactSDK) SDK. It contains the same React Context, Providers, and Hooks, used in the same way, HOWEVER you must import from the `statsig-react-native` package.
