import Tabs from "@theme/Tabs";
import TabItem from "@theme/TabItem";

<Tabs
  defaultValue="java"
  values={[
    {label: 'Java', value: 'java'},
    {label: 'Kotlin', value: 'kotlin'},
  ]}>
  <TabItem value="java">

```java
DynamicConfig expConfig = Statsig.getConfig("new_user_promo");

String promoTitle = expConfig.getString("title", "Welcome to Statsig! Use discount code WELCOME10OFF for 10% off your first purchase!");
Double discount = expConfig.getDouble("discount", 0.1);

...

Double price = msrp * (1 - discount);
```

  </TabItem>
  <TabItem value="kotlin">

```kotlin
val expConfig = Statsig.getConfig("new_user_promo")

val promoTitle = expConfig.getString("title", "Welcome to Statsig! Use discount code WELCOME10OFF for 10% off your first purchase!")
val discount = expConfig.getDouble("discount", 0.1)

...

val price = msrp * (1 - discount);
```

  </TabItem>
</Tabs>
