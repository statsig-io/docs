import CodeBlock from "@theme/CodeBlock";

export const Snippets = {
  // Gates
  gateSnippet: (
    <CodeBlock language="jsx">
      {`const result = await statsig.checkGateWithExposureLoggingDisabled('a_gate_name');`}
    </CodeBlock>
  ),
  gateExposureSnippet: (
    <CodeBlock language="jsx">
      {`statsig.manuallyLogGateExposure('a_gate_name');`}
    </CodeBlock>
  ),
  // Configs
  configSnippet: (
    <CodeBlock language="jsx">
      {`const config = await statsig.getConfigWithExposureLoggingDisabled('a_dynamic_config_name');`}
    </CodeBlock>
  ),
  configExposureSnippet: (
    <CodeBlock language="jsx">
      {`statsig.manuallyLogConfigExposure('a_dynamic_config_name');`}
    </CodeBlock>
  ),
  // Experiment
  experimentSnippet: (
    <CodeBlock language="jsx">
      {`const experiment = await statsig.getExperimentWithExposureLoggingDisabled('an_experiment_name');`}
    </CodeBlock>
  ),
  experimentExposureSnippet: (
    <CodeBlock language="jsx">
      {`statsig.manuallyLogExperimentExposure('an_experiment_name');`}
    </CodeBlock>
  ),
  // Layer
  layerSnippet: (
    <CodeBlock language="jsx">
      {`const layer = await statsig.getLayerWithExposureLoggingDisabled('a_layer_name');
const paramValue = layer.get('a_param_name', 'fallback_value');`}
    </CodeBlock>
  ),
  layerExposureSnippet: (
    <CodeBlock language="jsx">
      {`statsig.manuallyLogLayerParameterExposure('a_layer_name', 'a_param_name');`}
    </CodeBlock>
  ),
};
