```python
# Get a dynamic config for a specific user
user = %StatsigUser{
    user_id: "test_user_123"
}
config = Statsig.get_config("a_config", user)

# Access the values in the dynamic config:

param_value = DynamicConfig.get_param_value(config, "header_text")
```