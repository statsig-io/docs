```Python
# Values via get_layer
user = %StatsigUser{
    user_id: "test_user_123"
}

layer = Statsig.get_layer("user_promo_experiments", user)
title_string_value = Layer.get(layer, "title", "Welcome to Statsig!")
discount_float_value = Layer.get(layer, "discount", 0.1)

# Via get_experiment
experiment = Statsig.get_experiment("user_promo_experiment", user)
title_exp = Experiment.get_param_value(experiment, "new_user_promo_title")
```