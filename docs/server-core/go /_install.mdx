via the `go get` CLI:

```
go get github.com/statsig-io/private-statsig-server-core/statsig-go@latest
```

Or, add a dependency on the most recent version of the SDK in go.mod:

```
require (
  github.com/statsig-io/go-sdk v1.26.0
)
```

See the [Releases tab in GitHub](https://github.com/statsig-io/go-sdk/releases) for the latest versions.

Run the following commands to install the necessary binaries and set environment variables:

```
go run github.com/statsig-io/private-statsig-server-core/statsig-go
source /Users/REPLACE THIS WITH HOME DIR INSTRUCTIONS/.statsig_env
```

