```csharp
var user = new StatsigUser("user-id");
var config = statsig.GetConfig(user, "app_config");

var welcomeMessage = config.GetString("welcome_message", "Welcome!");
var maxRetries = config.GetInt("max_retries", 3);
var enableFeature = config.GetBool("enable_feature", false);
```
