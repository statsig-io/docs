## Migration Helper Tools

To assist with migration from legacy SDKs to Server Core SDKs, we provide the following resources:

- **Migration checklist**: A step-by-step guide to ensure a smooth transition
- **Compatibility layer**: Optional utilities to ease the transition process
- **Code examples**: Side-by-side comparisons of legacy and Core SDK implementations

<div>{props.migrationHelperContent}</div>
