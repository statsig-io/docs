## Common Changes for All Server Core SDKs

Server Core SDKs share common architectural changes from legacy SDKs:

- **Instance-based vs Static methods**: Core SDKs use instance methods rather than static/global methods
- **Unified interfaces**: Consistent API design across languages
- **Builder pattern**: Many Core SDKs use builder patterns for configuration and user creation

<div>{props.globalChangesContent}</div>
