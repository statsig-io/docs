## StatsigOptions Configuration Changes

The table below shows the mapping between legacy SDK options and Server Core SDK options:

| Legacy Option | Core Option | Notes |
|---------------|-------------|-------|
<div>{props.optionsMapping}</div>

### New Options in Server Core

Server Core SDKs introduce several new configuration options:

<div>{props.newOptions}</div>
