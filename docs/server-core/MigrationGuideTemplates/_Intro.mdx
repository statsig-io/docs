## Migrating from Legacy SDK to Core SDK

This guide will help you migrate from the legacy {props.language} Server SDK to the new {props.language} Core SDK. The {props.language} Core SDK offers significant performance improvements and new features, as it's built on a shared Rust core library.

<div>{props.introContent}</div>
