---
title: Logevent
description: ```tsx import { useStatsigClient } from '@statsig/react-bindings'; const { logEvent } = useStatsigClient(); return <button onClick={() => logEvent('my
---

```tsx
import { useStatsigClient } from '@statsig/react-bindings';

const { logEvent } = useStatsigClient();

return <button onClick={() => logEvent('my_event')}>Click Me</button>;
```

