---
title: Checkgate
description: ```csharp if (StatsigClient.CheckGate("new_homepage_design")) { // Gate is on, show new home page } else { // Gate is off, show old home page
---

```csharp
if (StatsigClient.CheckGate("new_homepage_design"))
{
  // Gate is on, show new home page
}
else
{
  // Gate is off, show old home page
}
```

