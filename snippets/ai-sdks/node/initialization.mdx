```js
import { Statsig } from "@statsig/statsig-node-core";
import { StatsigAI } from "@statsig/statsig-ai-node";

const statsig = new Statsig("YOUR_SERVER_SECRET_KEY", {
  environment: "production",
});
await statsig.initialize();

const statsigAI = new StatsigAI("YOUR_SERVER_SECRET_KEY", statsig);
await statsigAI.initialize();
```
