```js
// Create a user object
const user = new StatsigUser({ userID: "a-user" });

// Log the results of the eval
logEvalResult(user, "my_grader", "my_prompt", {
  score: 0.5,
  session_id: "1234567890",
  version: "my_version",
});
```