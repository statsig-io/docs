If you don’t have an existing Statsig instance, initialize the AI SDK with your sdkKey and statsigOptions. 
The AI SDK will manage its own lifecycle, including initialization, flushing, and shutdown.