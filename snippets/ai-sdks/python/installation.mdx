<CodeGroup>
```python pip
pip install statsig-ai
```

```python poetry
poetry add statsig-ai
```

```python pipenv
pipenv install statsig-ai
```
</CodeGroup>

If you have unique setup needs, take a look at the [Python Server SDK docs](/server-core/python-core#installation) - the AI SDK will install Python Server if you don't already have it.

