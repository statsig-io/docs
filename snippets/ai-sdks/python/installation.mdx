---
title: Installation
description: <CodeGroup> ```python pip pip install statsig-ai ``` ```python poetry poetry add statsig-ai ``` ```python pipenv pipenv install statsig-ai ``` </CodeG
---

<CodeGroup>
```python pip
pip install statsig-ai
```text

```python poetry
poetry add statsig-ai
```text

```python pipenv
pipenv install statsig-ai
```
</CodeGroup>
