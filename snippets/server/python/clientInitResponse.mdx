---
title: Clientinitresponse
description: ```python values = statsig.get_client_initialize_response(user); # dict() | None values = statsig.get_client_initialize_response(user=user, include_lo
---
```python
values = statsig.get_client_initialize_response(user); # dict() | None

# To apply local overrides, set include_local_overrides = True (python sdk v0.32.0+)

This page explains to apply local overrides, set include_local_overrides = true (python sdk v0.32.0+).

values = statsig.get_client_initialize_response(user=user, include_local_overrides=True); # dict() | None
```