---
title:  Getconfig
description: ```erlang User = #{<<"userID">> => UserID}, Config = statsig:get_config(User, ConfigName), ```text
---

```erlang
User = #{<<"userID">> => UserID},
Config = statsig:get_config(User, ConfigName),
```
