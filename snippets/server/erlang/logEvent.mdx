---
title: Logevent
description: <CodeGroup> ```erlang Erlang statsig:log_event(User, <<"event_name">>). ``` ```elixir Elixir Statsig.log_event(user, "event_name") ``` </CodeGroup>
---

<CodeGroup>
```erlang Erlang
statsig:log_event(User, <<"event_name">>).
```text

```elixir Elixir
Statsig.log_event(user, "event_name")
```
</CodeGroup>

