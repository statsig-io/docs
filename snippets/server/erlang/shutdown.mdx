---
title: Shutdown
description: <CodeGroup> ```erlang Erlang statsig:shutdown(). ``` ```elixir Elixir Statsig.shutdown() ``` </CodeGroup>
---

<CodeGroup>
```erlang Erlang
statsig:shutdown().
```text

```elixir Elixir
Statsig.shutdown()
```
</CodeGroup>

