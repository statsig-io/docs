---
title:  Checkgate
description: ```erlang User = #{<<"userID">> => UserID}, GateVal = statsig:check_gate(User, GateName), ```text
---

```erlang
User = #{<<"userID">> => UserID},
GateVal = statsig:check_gate(User, GateName),
```
