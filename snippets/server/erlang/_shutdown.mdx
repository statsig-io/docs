---
title:  Shutdown
description: ```erlang statsig.flush() ``` Optionally followed by shutting down the application: ```erlang application:stop(statsig) ```text
---

```erlang
statsig.flush()
```text

Optionally followed by shutting down the application:

```erlang
application:stop(statsig)
```