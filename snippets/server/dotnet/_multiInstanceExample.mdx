---
title:  Multiinstanceexample
description: ```csharp ServerDriver sdkInstance = new ServerDriver(secretKey, options); await serverDriver.Initialize().ConfigureAwait(false); ```text
---

```csharp
ServerDriver sdkInstance = new ServerDriver(secretKey, options);
await serverDriver.Initialize().ConfigureAwait(false);
```
