---
title: Shutdown
description: ```rust statsig.shutdown().await?; ``` Alternatively, you can manually flush events without shutting down: ```rust // Manually flush events to the ser
---

```rust
statsig.shutdown().await?;
```text

Alternatively, you can manually flush events without shutting down: 

```rust
// Manually flush events to the server
statsig.flush_events().await;
```