---
title: Getexperiment
description: ```php $user = StatsigUserBuilder::withUserID('my_user')->build(); $xp = $statsig->getExperiment($user, 'an_experiment'); ```
---

```php
$user = StatsigUserBuilder::withUserID('my_user')->build();
$xp = $statsig->getExperiment($user, 'an_experiment');
```