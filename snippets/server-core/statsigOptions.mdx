## Statsig Options
You can pass in an optional parameter `options` in addition to `sdkKey` during initialization to customize the Statsig client. Here are the available options that you can configure.

Depending on the SDK, this section also documents one of three network-routing paths: a dedicated outbound HTTP proxy config, Statsig Forward Proxy configuration, or custom endpoint overrides for Statsig network calls.
