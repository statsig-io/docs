---
title: Checkgate
description: ```python user = StatsigUser("a-user") if statsig.check_gate(user, "a_gate"): else: ```text
---

```python
user = StatsigUser("a-user")

if statsig.check_gate(user, "a_gate"):
    # Gate is on, enable new feature
else:
    # Gate is off
```
