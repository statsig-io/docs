---
title: Getprompt
description: ```python config = statsig.get_prompt(StatsigUser("my_user"), "ai_prompt_name") ```text
---
```python
# Get a prompt for a specific user

This page explains get a prompt for a specific user.

config = statsig.get_prompt(StatsigUser("my_user"), "ai_prompt_name")
```