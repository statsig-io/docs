---
title: Getpromptset
description: ```python prompt_set = statsig.get_prompt_set(user, "ai_prompt_name") live_prompt = prompt_set.get_live() ```text
---

```python
# Get the prompt set
prompt_set = statsig.get_prompt_set(user, "ai_prompt_name")

# live prompt
live_prompt = prompt_set.get_live()
```

